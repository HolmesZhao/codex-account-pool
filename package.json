{
  "name": "codex-account-pool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*"],
  "scripts": {
    "start": "npm run start --workspace @codex-pool/server",
    "dev": "npm run dev --workspace @codex-pool/web",
    "build": "npm run build --workspace @codex-pool/web",
    "test": "npm run test --workspaces --if-present",
    "migrate": "node ./scripts/migrate-from-asset-center.mjs",
    "self-test": "node ./scripts/self-test.mjs"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
